{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "bundle": {
      "dependsOn": ["^bundle"],
      "outputs": ["dist/**"]
    },
    "compile": {
      "dependsOn": ["^compile"],
      "outputs": ["out/**"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}