/**
 * Original work Copyright (c) 2022 Signum Network
 */

/**
 * Parses a hexadecimal transaction byte string to a human readable json
 * @param transactionBytes
 * @return The result as object
 */
export function parseTransactionBytes(transactionBytes: string) {
    return {
        foo: 'bar'
    }
}
